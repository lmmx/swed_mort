from ..preproc import all_countries_out_prefix, swed_out_prefix
from ..preproc.data import dir_path as data_dir

mort_swed = data_dir / f"{swed_out_prefix}.tsv"
fn_swed = data_dir / f"{swed_out_prefix}.tsv"
mort_all = data_dir / f"{all_countries_out_prefix}.tsv"
fn_all = data_dir / f"{all_countries_out_prefix}.tsv"
